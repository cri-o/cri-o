---
- name: clone runc source repo
  git:
    repo: "https://github.com/kolyshkin/runc.git"
    dest: "{{ ansible_env.GOPATH }}/src/github.com/kolyshkin/runc"
    version: "{{ runc_git_version }}"

- name: clean runc build directory
  command: make clean
  args:
    chdir: "{{ ansible_env.GOPATH }}/src/github.com/kolyshkin/runc"

- name: build runc
  make:
    chdir: "{{ ansible_env.GOPATH }}/src/github.com/kolyshkin/runc"

- name: install runc
  make:
    target: "install"
    chdir: "{{ ansible_env.GOPATH }}/src/github.com/kolyshkin/runc"

- name: link runc
  file:
    src: /usr/local/sbin/runc
    dest: /usr/bin/runc
    state: link
    force: yes
